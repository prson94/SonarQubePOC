namespace d360.core.enums
{
    public enum HashAlgorithmType
    {
        None = 0, // Used as a placeholder value, to configure settings for OpenId.
        SHA1 = 1,
        SHA256 = 2,
        SHA224 = 3,
        SHA384 = 4,
        SHA512 = 5
    }
}
