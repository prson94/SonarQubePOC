@{
    Layout = "~/Views/Shared/_Base.cshtml";
}
<h1>You have reached this page in error.</h1>
@if (Request.IsLocal && Server.GetLastError() != null)
{
<text>
    <div>@Server.GetLastError().Message</div>
</text>
}