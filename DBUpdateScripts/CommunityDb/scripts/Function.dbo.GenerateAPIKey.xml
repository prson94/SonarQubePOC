<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Core" id="Function.dbo.GenerateAPIKey-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
execute dbo.sp_executesql @statement = N'create or alter function [dbo].[GenerateAPIKey]
(
    @Length int
)
RETURNS varchar(max)
AS
BEGIN
	DECLARE @RandomID varchar(max)
	DECLARE @counter smallint
	DECLARE @RandomNumber float
	DECLARE @RandomNumberInt tinyint
	DECLARE @CurrentCharacter varchar(1)
	DECLARE @ValidCharacters varchar(255)
	SET @ValidCharacters = ''ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-=+$!''
	DECLARE @ValidCharactersLength int
	SET @ValidCharactersLength = len(@ValidCharacters)
	SET @CurrentCharacter = ''''
	SET @RandomNumber = 0
	SET @RandomNumberInt = 0
	SET @RandomID = ''''

	SET @counter = 1

	WHILE @counter < (@Length + 1)
		BEGIN
			SELECT @RandomNumber = r FROM RandomNumber
			SET @RandomNumberInt = Convert(tinyint, ((@ValidCharactersLength - 1) * @RandomNumber + 1))

			SELECT @CurrentCharacter = SUBSTRING(@ValidCharacters, @RandomNumberInt, 1)

			SET @counter = @counter + 1

			SET @RandomID = @RandomID + @CurrentCharacter
		END
	
	RETURN @RandomID
END'
		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>