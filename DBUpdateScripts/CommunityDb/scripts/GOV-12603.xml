<databaseChangeLog
    
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="kmcnamee" id="GOV-12603-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
		IF (NOT EXISTS(SELECT 1 FROM [dbo].[Setting] WHERE [ID] = 75))
		BEGIN
			insert into [Setting] (ID, FieldName, Name, Description, DefaultValue, SettingType)
			VALUES(75, 'EnableJsonAttribute', 'EnableJsonAttribute', 'Enable JSON Attribute field type support.  Enabling this stores all properties for all JSON fields in the govern environment as well as enabling this field type. Default false.  Changing this value requires reloading affected data to parse json properties.', 'false', 0)
		END
		]]>
    </sql>
	<rollback>
	<![CDATA[	
		delete from companysetting where settingid = 75
		delete from setting where id = 75
		]]>
	</rollback>
  </changeSet>


  <changeSet author="kmcnamee" id="GOV-12603-existingusage">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
      <![CDATA[
		IF((SELECT @@SERVERNAME) = 'bzbdz2ikmp')
BEGIN    
	DECLARE @companyId int
    DECLARE @companyIDs table(companyId int)
	    INSERT INTO @companyIDs VALUES 
	    (56),(86),(98),(112),(172),(187),(199),(216),(272),(320),(321),(322),(350),(357),(378),(384);

    DECLARE COMPANY_CURSOR CURSOR 
	    LOCAL STATIC READ_ONLY FORWARD_ONLY
    FOR 
    SELECT DISTINCT CompanyId 
    FROM @companyIDs
    OPEN COMPANY_CURSOR
    FETCH NEXT FROM COMPANY_CURSOR INTO @companyId
    WHILE @@FETCH_STATUS = 0
    BEGIN 
	    IF NOT EXISTS (SELECT 1 FROM CompanySetting WHERE CompanyID=@companyId AND SettingID=75)
	    BEGIN
		    INSERT INTO [dbo].[CompanySetting] VALUES (@companyId, 75, 'true')			
	    END			
	    FETCH NEXT FROM COMPANY_CURSOR INTO @companyId
    END
    CLOSE COMPANY_CURSOR
    DEALLOCATE COMPANY_CURSOR 
END

		]]>
    </sql>    
  </changeSet>
  
  </databaseChangeLog>
