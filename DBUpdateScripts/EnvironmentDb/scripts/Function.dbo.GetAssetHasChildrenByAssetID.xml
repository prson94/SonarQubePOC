<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Core" id="Function.dbo.GetAssetHasChildrenByAssetID-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
CREATE OR ALTER FUNCTION [dbo].[GetAssetHasChildrenByAssetID]
(
	@assetId bigint,
	@predicateType int
)
RETURNS TABLE 
AS
RETURN 
(
	select	
	case 
            when count(1) > 0 then cast(1 as bit) 
            else cast(0 as bit) 
        end as HasChildren
				    from	dbo.Asset A
							inner join dbo.[Intersect] I on I.Subject = A.Object and I.SubjectID = A.ObjectID
							inner join dbo.[IntersectType] IT on I.IntersectTypeID = IT.ID
							inner join dbo.[Predicate] P on P.ID = IT.PredicateID and P.Type <> @predicateType
                            inner join dbo.Asset IA on IA.Object = I.Object and IA.ObjectID = I.ObjectID 
                            inner join dbo.AssetType IAT on IAT.ID = IA.AssetTypeID                            
					where A.ID = @assetId
)

		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>