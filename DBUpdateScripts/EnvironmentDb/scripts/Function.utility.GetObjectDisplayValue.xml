<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="TKlok" id="Function.utility.GetObjectDisplayValue-v3">
    <comment>GOV-9617</comment>
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
CREATE OR ALTER FUNCTION [utility].[GetObjectDisplayValue]
(
	@Object varchar(50),
	@ObjectID int,
	@ObjectTypeID int	
)
RETURNS nvarchar(max)
AS
BEGIN
	declare @formattedValue nvarchar(max)
	declare @tokens table(ID int identity(1,1), Token nvarchar(100), Field nvarchar(100))
	declare @fieldValues table(Field nvarchar(100), Value nvarchar(max))
	declare @displayFormat nvarchar(250)

	if @Object = 'Artifact'
	begin
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'ArtifactType');
	end	
	if @Object = 'Policy'
	begin		
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'PolicyType');
	end
	if @Object = 'ReferenceItem'
	begin		
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'ReferenceItemType');

		insert into @fieldValues
			SELECT 'Code',
					Code
			FROM	Asset 
			WHERE	[Object]='ReferenceItem'  and [ObjectID] = @ObjectID
	end
	if @Object = 'Rule'
	begin		
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'RuleType');
	end
	if @Object = 'Taxonomy'
	begin
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'TaxonomyType');		
	end

	set @formattedValue = @displayFormat

	while patindex('%{%',@formattedValue) > 0
	begin
		declare @txt nvarchar(100) = SUBSTRING(@formattedValue, patindex('%{%',@formattedValue), PATINDEX('%}%', @formattedValue))
		insert into @tokens Values (@txt, REPLACE(REPLACE(@txt,'{',''),'}',''))
		set @formattedValue = replace(@formattedValue, @txt, '')
	end

	insert into @fieldValues
		SELECT	FT.Name,
				F.FormattedValue
		FROM	Field F
				inner join FieldType FT on FT.ID = F.FieldTypeID and F.ObjectType = @Object and F.ObjectID = @ObjectID
	
	declare @current int,
			@max int

	set @current = 1
	select @max = Max(ID) from @tokens
	set @formattedValue = @displayFormat

	while(@current <= @max)
	begin
		declare @currentToken nvarchar(100) = null,
				@currentField nvarchar(100) = null,
				@currentValue nvarchar(max) = null,
				@lkpType nvarchar(250) = null, 
				@lkpID int = null, 
				@lkpFormat nvarchar(250) = null

		select	@currentField = Field, 
				@currentToken = Token 
		from	@tokens
		where	ID = @current

		select	@currentValue = Value
		from	@fieldValues 
		where	Field = @currentField

		if @currentValue is not null
		begin
			SET @formattedValue = REPLACE(@formattedValue, @currentToken, @currentValue)
		end
		else
		begin
			SET @formattedValue = REPLACE(@formattedValue, @currentToken, '')
		end

		SET @current = @current + 1
	end

	return @formattedValue
END
		]]>
    </sql>
	<rollback>
		<![CDATA[	
CREATE OR ALTER FUNCTION [utility].[GetObjectDisplayValue]
(
	@Object varchar(50),
	@ObjectID int,
	@ObjectTypeID int	
)
RETURNS nvarchar(max)
AS
BEGIN
	declare @formattedValue nvarchar(max)
	declare @tokens table(ID int identity(1,1), Token nvarchar(100), Field nvarchar(100))
	declare @fieldValues table(Field nvarchar(100), Value nvarchar(max))
	declare @displayFormat nvarchar(250)

	if @Object = 'Artifact'
	begin
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'ArtifactType');
	end	
	if @Object = 'FusionQueryAttribute'
	begin
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'FusionQueryAttributeType');
	end
	if @Object = 'Policy'
	begin		
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'PolicyType');
	end
	if @Object = 'ReferenceItem'
	begin		
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'ReferenceItemType');

		insert into @fieldValues
			SELECT 'Code',
					Code
			FROM	Asset 
			WHERE	[Object]='ReferenceItem'  and [ObjectID] = @ObjectID
	end
	if @Object = 'Rule'
	begin		
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'RuleType');
	end
	if @Object = 'Taxonomy'
	begin
		set @displayFormat = (select DisplayFormat from [AssetType] where ObjectID = @ObjectTypeID and [Object] = 'TaxonomyType');		
	end

	set @formattedValue = @displayFormat

	while patindex('%{%',@formattedValue) > 0
	begin
		declare @txt nvarchar(100) = SUBSTRING(@formattedValue, patindex('%{%',@formattedValue), PATINDEX('%}%', @formattedValue))
		insert into @tokens Values (@txt, REPLACE(REPLACE(@txt,'{',''),'}',''))
		set @formattedValue = replace(@formattedValue, @txt, '')
	end

	insert into @fieldValues
		SELECT	FT.Name,
				F.FormattedValue
		FROM	Field F
				inner join FieldType FT on FT.ID = F.FieldTypeID and F.ObjectType = @Object and F.ObjectID = @ObjectID
	
	declare @current int,
			@max int

	set @current = 1
	select @max = Max(ID) from @tokens
	set @formattedValue = @displayFormat

	while(@current <= @max)
	begin
		declare @currentToken nvarchar(100) = null,
				@currentField nvarchar(100) = null,
				@currentValue nvarchar(max) = null,
				@lkpType nvarchar(250) = null, 
				@lkpID int = null, 
				@lkpFormat nvarchar(250) = null

		select	@currentField = Field, 
				@currentToken = Token 
		from	@tokens
		where	ID = @current

		select	@currentValue = Value
		from	@fieldValues 
		where	Field = @currentField

		if @currentValue is not null
		begin
			SET @formattedValue = REPLACE(@formattedValue, @currentToken, @currentValue)
		end
		else
		begin
			SET @formattedValue = REPLACE(@formattedValue, @currentToken, '')
		end

		SET @current = @current + 1
	end

	return @formattedValue
END
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>