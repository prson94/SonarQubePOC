<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="TDuric" id="Function.graph.GetPath-DependencyDrop-v4">
  		<preConditions onFail="MARK_RAN">      
			<sqlCheck expectedResult="0">SELECT count(1) FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA = 'dbo' AND TABLE_NAME = '_checkTable_GOV_14947'</sqlCheck>
		</preConditions>
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<comment>
			Have to drop this first as it is schema-bound and depends on function below. 
		</comment>
		<![CDATA[
drop view if exists graph.AssetNodeDisplayPath;
		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

  <changeSet author="TDuric" id="Function.graph.GetPath-v7">
  		<preConditions onFail="MARK_RAN">      
			<sqlCheck expectedResult="0">SELECT count(1) FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA = 'dbo' AND TABLE_NAME = '_checkTable_GOV_14947'</sqlCheck>
		</preConditions>
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<comment>Any time you update this function, you must also increment version of dependent item(s) listed in changeset above, and update the version of the above changeset.</comment>
		<![CDATA[
create or alter function [graph].[GetPath] 
(
	@segments xml,
	@levelDelimiter nvarchar(50),
	@keyDelimiter nvarchar(50)
)
returns nvarchar(850) WITH SCHEMABINDING
as
begin
	declare @path nvarchar(850)
	
	if @segments is null
	begin
		set @path = ''
	end
	else
	begin
		select	@path = string_agg(V, @levelDelimiter)
		from	(
				select	string_agg(Val, @keyDelimiter) within group(order by P) as  V
				from	(
						select	X.p.value('./@level', 'int') as L,
								X.p.value('./@position', 'int') as P,
								X.p.value('.', 'nvarchar(250)') as Val
						from	@segments.nodes('/path/segment') X(p)
						) O
				group by L
				) O
		
		if @path is null
		begin
			set @path = '[ERROR:KEY_FIELDS_NULL]'
		end
	end
	
	return @path
end
		]]>
    </sql>
	<rollback>
		<![CDATA[	
create or alter function [graph].[GetPath] 
(
	@segments xml,
	@levelDelimiter nvarchar(50),
	@keyDelimiter nvarchar(50)
)
returns nvarchar(850) WITH SCHEMABINDING
as
begin
	declare @path nvarchar(850)
	
	if @segments is null
	begin
		set @path = ''
	end
	else
	begin
		select	@path = string_agg(V, @levelDelimiter)
		from	(
				select	string_agg(Val, @keyDelimiter) within group(order by P) as  V
				from	(
						select	X.p.value('./@level', 'int') as L,
								X.p.value('./@position', 'int') as P,
								(select X.p.value('.', 'nvarchar(250)') for xml path('')) as Val
						from	@segments.nodes('/path/segment') X(p)
						) O
				group by L
				) O
		
		if @path is null
		begin
			set @path = '[ERROR:KEY_FIELDS_NULL]'
		end
	end
	
	return @path
end
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>
