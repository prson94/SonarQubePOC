<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="MDonato" id="Function.dbo.GetAssetScoreById-v3">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
create or alter function [dbo].[GetAssetScoreById]
(
	@Id bigint,
	@scoreType int
)
RETURNS TABLE 
AS
RETURN 
(
	select	top 1
			S.RunDate, 
			S.EffectiveDate, 
			S.[Value],
			case when S.Value is null then ' ' 
			else cast(cast((round(S.[Value] * 100, 1)) as float) as nvarchar) + '%'
			end as FormattedValue
	from	metrics.Score S
			inner join Asset A on A.[uid] = S.AssetUid and A.Id = @Id and S.EndDate is null
			inner join metrics.Allocation Al on Al.Uid = S.AllocationUid and Al.ScoreType = @scoreType and Al.OverrideName is null
)
		]]>
    </sql>
	<rollback>
		<![CDATA[	
create or alter function [dbo].[GetAssetScoreById]
(
	@Id bigint,
	@scoreType int
)
RETURNS TABLE 
AS
RETURN 
(
	select	top 1
			S.RunDate, 
			S.EffectiveDate, 
			S.[Value],
			case when S.Value is null then ' ' 
			else cast(cast((S.[Value] * 100) as float) as nvarchar) + '%'
			end as FormattedValue
	from	metrics.Score S
			inner join Asset A on A.[uid] = S.AssetUid and A.Id = @Id and S.EndDate is null
			inner join metrics.Allocation Al on Al.Uid = S.AllocationUid and Al.ScoreType = @scoreType and Al.OverrideName is null
)
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>