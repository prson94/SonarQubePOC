<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="bhoward" id="Function.dbo.GetAssetKeyHashById-v2">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
CREATE OR ALTER FUNCTION [dbo].[GetAssetKeyHashById](
      @id bigint
      )
      RETURNS TABLE
      AS
      RETURN
      (
		  select		
			  A.AssetTypeID,
			  A.ID,
			  CONVERT(
			  varchar(32),
			  SUBSTRING(HASHBYTES('SHA1', STRING_AGG(A.FieldTypeID + A.[Value], char(59))), 3, 32),
			  2) as KeyHash
		  from (
				select	B.ID, 
						B.AssetTypeID, 
						COALESCE(F.[Value], F.[FormattedValue]) as Value, 
						cast(FieldTypeID as nvarchar) + ':' as FieldTypeID
				from	Asset B
						inner join Field F on F.ObjectType = B.[Object] and F.ObjectID = B.ObjectID 
						inner join FieldType FT on FT.ID = F.FieldTypeID and FT.AssetTypeID = B.AssetTypeID and FT.IsPartOfKey = 1
				where	B.ID = @id
				
				union all
				
				select	C.ID, 
						C.AssetTypeID, 
						C.Code as [Value], 
						'' as FieldTypeID
				from	Asset C
				where	C.[Object] = 'ReferenceItem' 
						and C.ID = @id
		  ) A
		  group by	A.AssetTypeID, A.ID
      )
		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>