<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Puneet Gupta" id="Function.dbo.GetArtifactKeyHashByIdWithParent-v2">
  <comment>GOV-12916</comment>
  <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
if exists (select * from [INFORMATION_SCHEMA].[ROUTINES] where SPECIFIC_SCHEMA = 'dbo' and SPECIFIC_NAME = 'GetArtifactKeyHashByIdWithParent')
begin
	drop function [dbo].[GetArtifactKeyHashByIdWithParent]
end
		]]>
    </sql>
	<rollback>
		<![CDATA[	
CREATE OR ALTER FUNCTION [dbo].[GetArtifactKeyHashByIdWithParent](
	@Id bigint
)
RETURNS TABLE 
AS
RETURN 
(
		select	CONVERT(
					varchar(32), 
					SUBSTRING(HASHBYTES('SHA1', STRING_AGG(cast(A.FieldTypeID as nvarchar) + ':' +A.Value, char(59))), 3, 32), 
					2) as KeyHash
		from	(
				select		top 1000000000 
							A.AssetTypeID,
							A.ID,														
							FT.ID as FieldTypeID,
							F.Value as [Value]
				from		Asset A
				inner join Field F on F.ObjectType = A.Object and F.ObjectID = A.ObjectID and A.Object != 'ReferenceItem'
				inner join FieldType FT on FT.ID = F.FieldTypeID 
										and FT.AssetTypeID = A.AssetTypeID
										and FT.IsPartOfKey = 1
				where a.id = @id
				union
				select
					top 1
					A.AssetTypeID,
					A.ID,
					-1 as FieldTypeID,
					AD.DisplayValue as [Value]												
				from 	Asset A
						inner join [utility].IntersectAsset IAD on A.ID = IAD.ObjectAssetID and IAD.[Object] = 'Artifact'							
						inner join dbo.Asset IA on IA.Object = 'Artifact' and IA.ObjectID = IAD.SubjectID and IAD.PredicateType = 3
						inner join dbo.AssetType IAT on IAT.ID = IA.AssetTypeID
						inner join AssetDisplayValue AD on  AD.AssetID = IA.ID
				where 	a.id = @id	
				order by 	fieldtypeid
				) A
)
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>