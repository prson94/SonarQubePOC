<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Pappas" id="Procedure.dbo.SimpleAssetSearch-v2">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
create or alter procedure [dbo].[SimpleAssetSearch]
	@assetTypeId int,
	@filter nvarchar(500),
	@skip int,
	@take int
AS
BEGIN
	 select		Uid as [value],
				case 
					when len(DisplayPath) > 0 then DisplayPath
					else 'ERR:MISSING PATH SEGMENTS'
				end as [label]
	 from		graph.AssetNode
	 where		AssetTypeID = @assetTypeID 
				and DisplayPath like @filter
	 order by	DisplayPath asc
	 offset @skip rows fetch next @take rows only
END
		]]>
    </sql>
	<rollback>
		<![CDATA[	
CREATE OR ALTER procedure [dbo].[SimpleAssetSearch]
	@assetTypeId int,
	@filter nvarchar(500),
	@skip int,
	@take int
AS
BEGIN
  drop table if exists #tempTable
	 select
		 A.[UID] as [AssetUid],
		 KP.DisplayPath as [Path]
	 into #tempTable
	 from Asset A
	 inner join AssetType T on T.ID = A.AssetTypeID
	 inner join graph.AssetNodeDisplayPath KP on KP.ID = a.ID 
	 where A.AssetTypeID = @assetTypeID and KP.DisplayPath like @filter
	 order by KP.DisplayPath asc
	offset @skip rows fetch next @take rows only
            
	select AssetUid as value,
	case when len([Path]) > 0 then [Path]
	else 'ERR:MISSING PATH SEGMENTS'
	end as label 
	from #tempTable
END
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>
