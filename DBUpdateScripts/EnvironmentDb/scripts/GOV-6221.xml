<databaseChangeLog
    
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="BHoward" id="GOV-6221">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="true">
      BEGIN
        IF NOT EXISTS (SELECT 1 FROM [dbo].[RuleDimension] WHERE [Name] = 'Completeness')
        BEGIN
        INSERT [dbo].[RuleDimension] VALUES (N'Completeness', N'Is all the requisite information available? Are data values missing, or in an unusable state? In some cases, missing data is irrelevant, but when the information that is missing is critical to a specific business process, completeness becomes an issue. ', 1, getutcdate(), 0)
        END
        IF NOT EXISTS (SELECT 1 FROM [dbo].[RuleDimension] WHERE [Name] = 'Conformity')
        BEGIN
        INSERT [dbo].[RuleDimension] VALUES (N'Conformity', N'Are there expectations that data values conform to specified formats? If so, do all the values conform to those formats? Maintaining conformance to specific formats is important in data representation, presentation, aggregate reporting, search, and establishing key relationships.', 1, getutcdate(), 0)
        END
        IF NOT EXISTS (SELECT 1 FROM [dbo].[RuleDimension] WHERE [Name] = 'Consistency')
        BEGIN
        INSERT [dbo].[RuleDimension] VALUES (N'Consistency', N'Do distinct data instances provide conflicting information about the same underlying data object? Are values consistent across data sets? Do interdependent attributes always appropriately reflect their expected consistency? Inconsistency between data values plagues organizations attempting to reconcile between different systems and applications.', 1, getutcdate(), 0)
        END
        IF NOT EXISTS (SELECT 1 FROM [dbo].[RuleDimension] WHERE [Name] = 'Accuracy')
        BEGIN
        INSERT [dbo].[RuleDimension] VALUES (N'Accuracy', N'Do data objects accurately represent the “real-world” values they are expected to model? Incorrect spellings of product or person names, addresses, and even untimely or not current data can impact operational and analytical applications.', 1, getutcdate(), 0)
        END
        IF NOT EXISTS (SELECT 1 FROM [dbo].[RuleDimension] WHERE [Name] = 'Duplication')
        BEGIN
        INSERT [dbo].[RuleDimension] VALUES (N'Duplication', N'Are there multiple, unnecessary representations of the same data objects within your data set? The inability to maintain a single representation for each entity across your systems poses numerous vulnerabilities and risks.', 1, getutcdate(), 0)
        END
        IF NOT EXISTS (SELECT 1 FROM [dbo].[RuleDimension] WHERE [Name] = 'Integrity')
        BEGIN
        INSERT [dbo].[RuleDimension] VALUES (N'Integrity', N'What data is missing important relationship linkages? The inability to link related records together may actually introduce duplication across your systems. Not only that, as more value is derived from analyzing connectivity and relationships, the inability to link related data instance together impedes this valuable analysis.', 1, getutcdate(), 0)
        END
      END
    </sql>
    <rollback>
      begin
      DELETE FROM [dbo].[RuleDimension] WHERE [Name] = 'Completeness'
      DELETE FROM [dbo].[RuleDimension] WHERE [Name] = 'Conformity'
      DELETE FROM [dbo].[RuleDimension] WHERE [Name] = 'Consistency'
      DELETE FROM [dbo].[RuleDimension] WHERE [Name] = 'Accuracy'
      DELETE FROM [dbo].[RuleDimension] WHERE [Name] = 'Duplication'
      DELETE FROM [dbo].[RuleDimension] WHERE [Name] = 'Integrity'
      end
    </rollback>
  </changeSet>
</databaseChangeLog>
