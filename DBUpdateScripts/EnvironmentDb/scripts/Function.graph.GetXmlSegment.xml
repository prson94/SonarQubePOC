<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Core" id="Function.graph.GetXmlSegment-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
CREATE OR ALTER FUNCTION [graph].[GetXmlSegment]
(
	@xml xml,
	@assetTypeId int,
	@id bigint,
	@level int
)
RETURNS xml
AS
BEGIN
	if @xml is null
	begin
		set @xml = '<path></path>'
	end

	declare @segment xml;  
	
	set @segment =	(
					select		@level as '@level',
								ROW_NUMBER() over (order by T.ColumnOrder) as '@position',--T.ColumnOrder as '@position',
								@assetTypeId as '@assetTypeId',
								@id as '@assetId',
								FormattedValue as 'data()'
					from		Field F
								inner join FieldType T on T.ID = F.FieldTypeID and T.IsPartOfKey = 1 and F.AssetID = @id
					order by	T.ColumnOrder
					for xml path('segment')
					)

	SET @xml.modify('             
	insert sql:variable("@segment")             
	into (/path)[1]')

	return @xml
END
		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>