<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="bquinlivan" id="Procedure.graph.CreateAssetResult-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
-- =============================================
-- Author:      Brian Quinlivan
-- Create Date: 2020-03-24
-- Description: Creates Data Quality result record and link to asset and/or rule
-- =============================================
CREATE OR ALTER PROCEDURE [graph].[CreateAssetResult]
(    
	@EffectiveDate DATETIME, 
	@RunDate DATETIME,
	@PassCount INT,
    @FailCount INT,
    @UserId INT,
	@evaluatedAssetUid UNIQUEIDENTIFIER,
	@ownedAssetUid UNIQUEIDENTIFIER
)
AS
BEGIN	
    -- SET NOCOUNT ON added to prevent extra result sets from
    -- interfering with SELECT statements.
    SET NOCOUNT ON
	declare @fromAssetResultNodeId NVARCHAR(MAX), @toAssetNodeId NVARCHAR(MAX), @Uid UNIQUEIDENTIFIER = null, @success INT, @message varchar(200)
	set @success = 0
	
	IF @ownedAssetUid is not null
	BEGIN				
		BEGIN TRY
		BEGIN TRANSACTION
			--IF @Uid is null
			BEGIN
				set @Uid = NEWID();
		
				INSERT INTO [dbo].[AssetResult]
						([Uid]
						,[EffectiveDate]
						,[RunDate]
						,[PassCount]
						,[FailCount]
						,[CreatedOn]
						,[CreatedBy]
						,[UpdatedOn]
						,[UpdatedBy])
					VALUES
						(@Uid
						,@EffectiveDate
						,@RunDate
						,@PassCount
						,@FailCount
						,GETDATE()
						,@UserId
						,GETDATE()
						,@UserId)
					   
				-- if node record was created then create edge record.
				IF @@ROWCOUNT >= 1
				BEGIN		

					SELECT @fromAssetResultNodeId = $node_id FROM AssetResult WHERE [Uid] = @Uid

					SELECT @toAssetNodeId = $node_Id FROM graph.AssetNode WHERE [Uid] = @ownedAssetUid

					INSERT INTO [dbo].[AssetResultEdge]
								($from_id           
								,$to_id
								,[Class])
							VALUES
								(@toAssetNodeId,
								@fromAssetResultNodeId,
								1)

						
					--if evaluated Uid Passed then create additional edge record
					IF (@evaluatedAssetUid is not null)
					BEGIN
						SELECT @toAssetNodeId = $node_Id FROM graph.AssetNode WHERE [Uid] = @evaluatedAssetUid

						INSERT INTO [dbo].[AssetResultEdge]
								($from_id           
								,$to_id
								,[Class])
							VALUES
								(@toAssetNodeId,
								@fromAssetResultNodeId,
								2)
					END
						
					SET @success = 1
						
				END
			END
		COMMIT TRANSACTION
		END TRY
		BEGIN CATCH
			IF @@trancount > 0 ROLLBACK TRANSACTION
		END CATCH

		select @Uid as 'Uid', @success as 'Success', @message as 'Message'
	END
END

		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>