<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="kmcnamee" id="Table.reporting.Global_FieldAudit-v2">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA = 'reporting' AND TABLE_NAME = 'Global_FieldAudit')
BEGIN
	CREATE TABLE [reporting].[Global_FieldAudit](
		[AuditID] [bigint] NOT NULL,
		[FieldTypeID] [int] NOT NULL,
		[FieldName] [nvarchar](250) NOT NULL,
		[Version] [int] NOT NULL,
		[Value] [nvarchar](max) NULL,
    [PreviousValue] [nvarchar](max) NULL,
		CONSTRAINT [PK_ReportingFieldAudit] PRIMARY KEY CLUSTERED ( [AuditID] ASC, [FieldTypeID] ASC, [FieldName] ASC, [Version] DESC )
	)
END

IF EXISTS (
  SELECT *
  FROM   sys.columns
  WHERE  object_id = OBJECT_ID(N'[reporting].[FieldAudit]')
  AND name = 'PreviousValue'
  )
  BEGIN
    ALTER TABLE [reporting].[FieldAudit] ADD [PreviousValue] [nvarchar](max) NULL 
END

		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>