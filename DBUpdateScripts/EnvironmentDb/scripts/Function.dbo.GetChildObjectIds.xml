<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Puneet Gupta" id="Function.dbo.GetChildObjectIds-v2">
  <comment>GOV-12916</comment>
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
if exists (select * from [INFORMATION_SCHEMA].[ROUTINES] where SPECIFIC_SCHEMA = 'dbo' and SPECIFIC_NAME = 'GetChildObjectIds')
begin
	drop function [dbo].[GetChildObjectIds]
end
		]]>
    </sql>
	<rollback>
		<![CDATA[	
CREATE OR ALTER FUNCTION [dbo].[GetChildObjectIds]
(
	@type varchar(50),
	@id int
)
RETURNS TABLE
AS
RETURN
(
	select I.ObjectID as ChildID from Asset A
	inner join AssetType ST on ST.ID = A.AssetTypeID
	inner join [IntersectType] T on T.[Subject] = ST.[Object] and T.SubjectID = ST.ObjectID
	inner join [Predicate] P on P.ID = T.PredicateID and P.[Type] = 3
	inner join [Intersect] I on I.Subject = @type and I.SubjectID = @id and I.IntersectTypeID = T.ID
	where A.[Object] = @type and A.ObjectID = @id
)
		]]>
	</rollback>
  </changeSet>  
</databaseChangeLog>