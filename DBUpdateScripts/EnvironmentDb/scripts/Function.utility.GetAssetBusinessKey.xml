<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Core" id="Function.utility.GetAssetBusinessKey-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
CREATE OR ALTER FUNCTION [utility].[GetAssetBusinessKey]
(
	@ID int
)
RETURNS TABLE
AS
RETURN 
(
	select		@ID as ID,
				STRING_AGG(Value, '-') as [Key]
	from		(
				select		top 100 percent
							A.Code as Value
				from		Asset A
				where		A.Object = 'ReferenceItem' and A.ID = @ID				
				union
				select		*
				from		(
							select		top 100 percent
										coalesce(F.FormattedValue, '') as Value
							from		Asset A
										inner join AssetType T on T.ID = A.AssetTypeID and A.ID = @ID
										inner join Field F on F.ObjectType = A.Object and F.ObjectID = A.ObjectID
										inner join FieldType FT on FT.ID = F.FieldTypeID 
																and FT.Object = T.Object and FT.ObjectID = T.ObjectID
																and FT.IsPartOfKey = 1
							order by	FT.ID
							) F
				) A
)
		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>