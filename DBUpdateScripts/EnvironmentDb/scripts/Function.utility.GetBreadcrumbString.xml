<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Puneet Gupta" id="Function.utility.GetBreadcrumbString-Drop-v1">
  <comment>GOV-11726</comment>
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
if exists (select * from [INFORMATION_SCHEMA].[ROUTINES] where SPECIFIC_SCHEMA = 'utility' and SPECIFIC_NAME = 'GetBreadcrumbString')
begin
	if exists (select * from [INFORMATION_SCHEMA].[ROUTINES] where SPECIFIC_SCHEMA = 'utility' and SPECIFIC_NAME = 'GetBreadcrumbStringWrapper')
	begin
		IF EXISTS (SELECT f.name
					FROM sys.Computed_columns f
					inner join sys.objects o on f.object_id = o.object_id
					where SCHEMA_NAME(schema_id) = 'dbo' and o.name = 'FusionAttributeType' and F.name ='TextPath' 
					and f.is_computed = 1
				   )
		BEGIN	
			ALTER TABLE [dbo].[FusionAttributeType] DROP column [TextPath]
			
			ALTER TABLE [dbo].[FusionAttributeType] ADD [TextPath] nvarchar(1000)
		END
		
		IF EXISTS (SELECT f.name
					FROM sys.Computed_columns f
					inner join sys.objects o on f.object_id = o.object_id
					where SCHEMA_NAME(schema_id) = 'dbo' and o.name = 'FusionAttributeType_backup' and F.name ='TextPath' 
					and f.is_computed = 1
				   )
		BEGIN	
			ALTER TABLE [dbo].[FusionAttributeType_backup] DROP column [TextPath]
			
			ALTER TABLE [dbo].[FusionAttributeType_backup] ADD [TextPath] nvarchar(1000)
		END
		drop FUNCTION [utility].[GetBreadcrumbStringWrapper]
	END
	drop FUNCTION [utility].[GetBreadcrumbString]
END		
		]]>
    </sql>
	<rollback>
		<![CDATA[	
CREATE OR ALTER FUNCTION [utility].[GetBreadcrumbString]
(
	@Type varchar(50),
	@ID int,
	@Delimiter varchar(10)
)
RETURNS nvarchar(1000)
AS
BEGIN
	
	DECLARE @breadcrumb nvarchar(1000)

	

	IF (@Type = 'FusionAttribute')
	BEGIN
		WITH H (Name, ParentID, ID, [level])
		AS
		(
			SELECT	Name, 
					ParentID, 
					ID, 
					0
			FROM	FusionAttribute
			WHERE	ID = @ID		
			UNION ALL
			SELECT	P.Name, 
					P.ParentID, 
					P.ID, 
					C.[level] + 1
			FROM	FusionAttribute	P
					INNER JOIN H AS C ON C.ParentID = P.ID and c.[level] < 6
		)
	
		SELECT @breadcrumb =	COALESCE(@breadcrumb + @Delimiter, '') + H.Name
								FROM	H
								ORDER BY H.level DESC
	END

	IF (@Type = 'FusionAttributeType')
	BEGIN
		WITH H (Name, ParentID, ID, [level])
		AS
		(
			SELECT	Name, 
					ParentID, 
					ID, 
					0
			FROM	FusionAttributeType
			WHERE	ID = @ID		
			UNION ALL
			SELECT	P.Name, 
					P.ParentID, 
					P.ID, 
					C.[level] + 1
			FROM	FusionAttributeType	P
					INNER JOIN H AS C ON C.ParentID = P.ID and c.[level] < 6
		)
	
		SELECT @breadcrumb =	COALESCE(@breadcrumb + @Delimiter, '') + H.Name
								FROM	H
								ORDER BY H.level DESC

		SELECT	@breadcrumb = FT.Name + @Delimiter + @breadcrumb
		FROM	FusionAttributeType FAT
				inner join FusionType FT on FAT.FusionTypeID = FT.ID and FAT.ID = @ID
	END

	

	RETURN @breadcrumb
END
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>