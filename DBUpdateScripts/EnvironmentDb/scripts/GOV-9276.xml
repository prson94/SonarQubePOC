<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Tomislav Duric" id="GOV-9276-AddFields">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
      <![CDATA[
IF NOT EXISTS(SELECT 1 FROM sys.columns 
          WHERE Name = N'CompletedOn'
          AND Object_ID = Object_ID(N'dbo.Issue'))
BEGIN
    ALTER TABLE dbo.Issue ADD CompletedOn [datetime] null
END

IF NOT EXISTS(SELECT 1 FROM sys.columns 
          WHERE Name = N'CompletedBy'
          AND Object_ID = Object_ID(N'dbo.Issue'))
BEGIN
    ALTER TABLE dbo.Issue ADD CompletedBy [int] null
END
]]>
    </sql>
    <rollback>
      <![CDATA[
IF EXISTS(SELECT 1 FROM sys.columns 
          WHERE Name = N'CompletedOn'
          AND Object_ID = Object_ID(N'dbo.Issue'))
BEGIN
    ALTER TABLE dbo.Issue DROP COLUMN CompletedOn
END

IF EXISTS(SELECT 1 FROM sys.columns 
          WHERE Name = N'CompletedBy'
          AND Object_ID = Object_ID(N'dbo.Issue'))
BEGIN
    ALTER TABLE dbo.Issue DROP COLUMN CompletedBy
END
]]>
    </rollback>
  </changeSet>
  <changeSet author="Tomislav Duric" id="GOV-9276-PopulateExistingRows">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
      <![CDATA[
merge dbo.Issue I USING (
SELECT  *
FROM    (select WI.ObjectID, wi.CompletedOn, ROW_NUMBER() OVER (PARTITION BY WI.ObjectID ORDER BY WI.ObjectID) AS RowNumber from workflow.Item WI
			inner join dbo.Issue I on I.ID = WI.ObjectID
			where WI.Object ='Issue') AS a
where RowNumber = 1)WorkflowItems 
ON WorkflowItems.ObjectId = I.Id and I.CompletedOn is null and I.CompletedBy is null
when matched 
THEN UPDATE SET I.CompletedOn = WorkflowItems.CompletedOn,
				I.CompletedBy = 0;
]]>
    </sql>
    <rollback>
      <![CDATA[
]]>
    </rollback>
  </changeSet>
    <changeSet author="Tomislav Duric" id="GOV-9276-GetObjectStatisticProc">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
      <![CDATA[
ALTER procedure [tile].[GetObjectStatistics]
	@type varchar(50),
	@id int
AS
BEGIN
	declare @table table (Name nvarchar(250), Value varchar(250), [Group] varchar(25), Url varchar(250), MostRecent datetime, TypeID int)
	
	declare @assetUid uniqueidentifier;

	select @assetUid = [Uid] from dbo.Asset where Object = @type and ObjectID = @id;

	insert into @table
		select NULL, count(1), 'Followers', '', max(datecreated),null
		from	Follow F
		inner join reporting.Global_Resource R on R.ResourceID = F.ResourceID
		where	F.ObjectType = @type and F.ObjectID = @id
	
	insert into @table
		select	NULL, count(1), 'Comments', '', max(datecreated),null
		from	Comment C
				inner join CommentRelation R	on R.CommentID = C.ID and C.ParentID is null
												and R.ObjectType = @type and R.ObjectID = @id
                                                and C.ParentID is null
												and C.IsDeleted = 0

	insert into @table 
		select	NULL, cast(Value * 100 as int), 'Score', '', S.EffectiveDate ,null
		from	metrics.Score S
			inner join (
				select	max(EffectiveDate) as EffectiveDate
				from	metrics.Score
				where	AssetUid = @assetUid
						and EffectiveDate <= getutcdate()
			) M on M.EffectiveDate = S.EffectiveDate and S.AssetUid = @assetUid

	if @type = 'Artifact' or @type = 'Taxonomy' or @type = 'Rule' or @type = 'Policy'
	begin
		insert into @table 
			select 
				lower(c.childartifacttypename), 
				count(1),
				'Children',
				'',
				getutcdate(),
				c.ChildArtifactTypeID
			from 
				asset a
			cross apply [dbo].[GetArtifactChildByAssetID](a.id) c
			where a.objectid = @id and a.[object] = 'Artifact' group by c.childartifacttypename, c.ChildArtifactTypeID
		insert into @table
			select 
				'Issue',
				count(1),
				'Issues',	
				'',
				max(i.CreatedOn),
				null
			from workflow.item wi
				inner join issue i on (wi.objectid = i.id and wi.[object] = 'Issue')
			where 
				i.object = @type and i.objectid = @id and wi.completedon is null		
	end
	select * from @table
END
]]>
    </sql>
    <rollback>
      <![CDATA[
ALTER procedure [tile].[GetObjectStatistics]
	@type varchar(50),
	@id int
AS
BEGIN
	declare @table table (Name nvarchar(250), Value varchar(250), [Group] varchar(25), Url varchar(250), MostRecent datetime, TypeID int)
	
	declare @assetUid uniqueidentifier;

	select @assetUid = [Uid] from dbo.Asset where Object = @type and ObjectID = @id;

	insert into @table
		select NULL, count(1), 'Followers', '', max(datecreated),null
		from	Follow F
		inner join reporting.Global_Resource R on R.ResourceID = F.ResourceID
		where	F.ObjectType = @type and F.ObjectID = @id
	
	insert into @table
		select	NULL, count(1), 'Comments', '', max(datecreated),null
		from	Comment C
				inner join CommentRelation R	on R.CommentID = C.ID and C.ParentID is null
												and R.ObjectType = @type and R.ObjectID = @id
                                                and C.ParentID is null
												and C.IsDeleted = 0

	insert into @table 
		select	NULL, cast(Value * 100 as int), 'Score', '', S.EffectiveDate ,null
		from	metrics.Score S
			inner join (
				select	max(EffectiveDate) as EffectiveDate
				from	metrics.Score
				where	AssetUid = @assetUid
						and EffectiveDate <= getutcdate()
			) M on M.EffectiveDate = S.EffectiveDate and S.AssetUid = @assetUid

	if @type = 'Artifact' or @type = 'Taxonomy' or @type = 'Rule' or @type = 'Policy'
	begin
		insert into @table 
			select 
				lower(c.childartifacttypename), 
				count(1),
				'Children',
				'',
				getutcdate(),
				c.ChildArtifactTypeID
			from 
				asset a
			cross apply [dbo].[GetArtifactChildByAssetID](a.id) c
			where a.objectid = @id and a.[object] = 'Artifact' group by c.childartifacttypename, c.ChildArtifactTypeID
		insert into @table
			select 
				'Issue',
				count(1),
				'Issues',	
				'',
				max(i.CreatedOn),
				null
			from workflow.item wi
				inner join issue i on (wi.objectid = i.id and wi.[object] = 'Issue')
			where 
				i.object = @type and i.objectid = @id and completedon is null		
	end
	select * from @table
END
]]>
    </rollback>
  </changeSet>

</databaseChangeLog>