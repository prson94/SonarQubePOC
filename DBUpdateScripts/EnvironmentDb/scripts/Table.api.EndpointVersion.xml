<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="Core" id="Table.api.EndpointVersion-v1">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
IF NOT EXISTS (SELECT * FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_SCHEMA = 'api' AND TABLE_NAME = 'EndpointVersion')
BEGIN
	CREATE TABLE [api].[EndpointVersion](
		[ID] [int] IDENTITY(1,1) NOT NULL,
		[EndpointID] [int] NOT NULL,
		[UriPrefix] [varchar](100) NOT NULL,
		[MajorVersion] [int] NOT NULL,
		[MinorVersion] [int] NOT NULL,
		CONSTRAINT [PK_Api_EndpointVersion] PRIMARY KEY NONCLUSTERED ( [ID] ASC )
	)
END

IF NOT EXISTS (select * from sys.foreign_keys where name = 'FK_EndpointVersion_Endpoint')
BEGIN
	ALTER TABLE [api].[EndpointVersion]  WITH CHECK ADD  CONSTRAINT [FK_EndpointVersion_Endpoint] FOREIGN KEY([EndpointID]) REFERENCES [api].[Endpoint] ([ID]) ON DELETE CASCADE
	ALTER TABLE [api].[EndpointVersion] CHECK CONSTRAINT [FK_EndpointVersion_Endpoint]
END
		]]>
    </sql>
	<rollback>
		<![CDATA[	

		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>