<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd
    http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

  <changeSet author="MDonato" id="Function.utility.OrderedSplit-v2">
    <sql dbms="mssql"
            endDelimiter="\nGO"
            splitStatements="true"
            stripComments="false">
		<![CDATA[
CREATE or alter function [utility].[OrderedSplit]
(
@String varchar(max),
@Delimiter varchar(10)
)
RETURNS @Results TABLE ([index] INT, [value] varchar(4000))
AS
BEGIN
	DECLARE @spacerepl char(1) = char(254)
	DECLARE @i int = 1, @f_id int = 1, @j int
	IF @Delimiter = ' ' BEGIN
		SELECT @String = REPLACE(@String, @DELIMITER, @spacerepl)
		SELECT @DELIMITER = @spacerepl
	END
	IF ISNULL(@String, '') = '' RETURN
	WHILE @i <= DATALENGTH(@String)
	BEGIN
		SELECT @j = CHARINDEX(@Delimiter, @String, @i)
		IF @j = 0 SELECT @j = DATALENGTH(@String) + 1
			INSERT @Results SELECT @f_id, SUBSTRING(@String, @i, @j - @i)
			SELECT @i = @j + DATALENGTH(@Delimiter)
			SELECT @f_id = @f_id + 1
		END
	RETURN
END
GO
GO
		]]>
    </sql>
	<rollback>
		<![CDATA[	
CREATE or alter function [utility].[OrderedSplit]
(
@String varchar(max),
@Delimiter varchar(10)
)
RETURNS @Results TABLE ([index] INT, [value] varchar(4000))
AS
BEGIN
	DECLARE @spacerepl char(1)=char(254)
	DECLARE @i int = 1, @f_id int = 1, @j int
	IF @Delimiter=' ' BEGIN
		SELECT @String=REPLACE(@String,@DELIMITER,@spacerepl)
		SELECT @DELIMITER=@spacerepl
	END
	IF ISNULL(@String,'')='' RETURN
	WHILE @i <= LEN(@String)
	BEGIN
		SELECT @j = CHARINDEX(@Delimiter, @String, @i)
		IF @j = 0 SELECT @j = len(@String) + 1
			INSERT @Results SELECT @f_id, RTRIM(SUBSTRING(@String, @i, @j - @i))
			SELECT @i = @j + LEN(@Delimiter)
			SELECT @f_id = @f_id + 1
		END
	RETURN
END
GO
		]]>
	</rollback>
  </changeSet>

</databaseChangeLog>